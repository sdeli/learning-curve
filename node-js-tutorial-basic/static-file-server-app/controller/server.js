const startServer = require('./modules/start-server.js');
const routeHandler = require('./modules/route-handler.js')

startServer(routeHandler);

// case: static file is wrond
// case: page is wrong


// szep folder sstruct